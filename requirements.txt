# Aria Security — Python dependencies
flask
flask-socketio
pywebview
pefile
yara-python
onnxruntime
numpy
scikit-learn
pandas
psutil
winotify
# Phase 2 detection engine
lightgbm
lief>=0.14,<0.16  # Task 2 EMBER extractor targets the 0.15.x API
onnxmltools
python-tlsh  # optional (TLSH fuzzy hashing); needs a C++ build on Windows — layer degrades to imphash-only if absent
# dev
pytest
watchdog  # real-time file-system events (RansomProtection)
pydivert  # WinDivert SYN/scan defense (PSDS); optional, degrades gracefully
